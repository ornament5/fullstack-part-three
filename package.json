{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "Excercize-app for Fullstack Open 2020",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../../Part2/phonebook && npm run build --prod && cp -r build ../../Part3/phonebook-backend/",
    "logs:prod": "heroku logs --tail",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Zeljko Ilic",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
